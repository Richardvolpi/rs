For this version i have added 3 new useful txt script commnads.

IfNpcIsNotThereGoto(id,*label*)
WaitForMenu(wait)
TalkToUntilMenu(id)

Everything else remains the same.

It is the normal SBoT but runs off txt scripts aswell and has a built in OCR.

Zym + LogiK