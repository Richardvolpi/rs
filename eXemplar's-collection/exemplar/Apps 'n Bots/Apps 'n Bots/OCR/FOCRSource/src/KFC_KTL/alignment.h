#ifndef alignment_h
#define alignment_h

#include <limits.h>

// ----------
// Alignment
// ----------
enum TAlignment
{
	ALIGNMENT_MIN			= 1,
	ALIGNMENT_MAX			= 2,
	ALIGNMENT_MID			= 3,
	ALIGNMENT_FORCE_UINT	= UINT_MAX,
};

#endif // alignment_h
